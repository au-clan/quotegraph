[project]
name = "quotegraph"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jdk4py>=21.0.8.0",
    "pyspark>=3.5.7",
    "tld>=0.13.1",
]


[tool.setuptools]
packages = ["quotegraph"]

