[project]
name = "annotation-interface"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.30.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
